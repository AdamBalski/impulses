FROM node:20-alpine

WORKDIR /app

EXPOSE 3001

CMD ["sh", "-c", "cd /app/client-sdks/typescript && npm install && cd /app/ui && npm install --ignore-scripts && exec npm run dev -- --host 0.0.0.0 --port 3001"]
