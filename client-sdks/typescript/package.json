{
  "name": "@impulses/sdk-typescript",
  "version": "0.1.0",
  "description": "Impulses client SDK for TypeScript",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "clean": "rm -rf dist",
    "lint": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "prepare": "npm run build"
  },
  "keywords": [
    "impulses",
    "sdk",
    "metrics",
    "timeseries"
  ],
  "license": "MIT",
  "dependencies": {
    "cross-fetch": "^3.1.8"
  },
  "devDependencies": {
    "typescript": "^5.3.3",
    "vitest": "^1.2.2"
  }
}
