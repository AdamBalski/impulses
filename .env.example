# PostgreSQL Configuration
POSTGRES_DB=impulses
POSTGRES_USER=impulses_user
POSTGRES_PASSWORD=impulses_password

# Application Configuration
PORT=8000
ORIGIN=http://localhost:8000
SESSION_TTL_SEC=1800

# PostgreSQL Connection (auto-generated from above)
POSTGRES_CONN_JSON={"host": "postgres", "port": 5432, "database": "impulses", "user": "impulses_user", "password": "impulses_password"}

# Google OAuth2 Credentials (get from Google Cloud Console)
GOOGLE_OAUTH2_CREDS={"web":{"client_id":"YOUR_CLIENT_ID","client_secret":"YOUR_CLIENT_SECRET","auth_uri":"https://accounts.google.com/o/oauth2/auth","token_uri":"https://oauth2.googleapis.com/token"}}
