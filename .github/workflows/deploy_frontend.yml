name: Deploy Frontend

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  frontend:
    runs-on: ubuntu-latest

    steps:
      - name: Start SSH agent
        uses: webfactory/ssh-agent@v0.9.0
        with:
          ssh-private-key: ${{ secrets.SSH_PRIVATE_KEY }}

      - name: Trust deploy host key
        env:
          DEPLOY_HOST: ${{ secrets.REMOTE_HOST }}
          DEPLOY_PORT: ${{ secrets.REMOTE_PORT }}
        run: |
          mkdir -p ~/.ssh
          ssh-keyscan -p "${DEPLOY_PORT:-22}" "$DEPLOY_HOST" >> ~/.ssh/known_hosts

      - name: Deploy frontend image with docker-compose
        env:
          DEPLOY_HOST: ${{ secrets.REMOTE_HOST }}
          DEPLOY_PORT: ${{ secrets.REMOTE_PORT }}
          DEPLOY_USER: ${{ secrets.REMOTE_USERNAME }}
          SHA: ${{ github.sha }}
        run: |
          ssh -p "${DEPLOY_PORT:-22}" "${DEPLOY_USER}@${DEPLOY_HOST}" "bash -lc 'set -euo pipefail; SHA=\"${SHA}\"; VERSIONS_FILE=/home/${DEPLOY_USER}/infra/compose/versions/impulses; echo \"IMPULSES_UI_VERSION=\$SHA\" >> \"\$VERSIONS_FILE\"; set -a; . \"\$VERSIONS_FILE\"; set +a; docker compose -f /home/${DEPLOY_USER}/infra/compose/impulses.yml up -d --wait impulses-ui'"
